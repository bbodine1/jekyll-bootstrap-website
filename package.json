{
  "name": "jekyll-bootstrap-website",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "Brad Bodine <bbodine1@github.com>",
  "license": "ISC",
  "devDependencies": {
    "bootstrap": "^4.1.2"
  },
  "scripts": {
    "prestart": "curl -o src/_data/test.json https://jsonplaceholder.typicode.com/posts",
    "start": "bundle exec jekyll serve --port 4000 --livereload",
    "build": "bundle exec jekyll build",
    "clean": "git clean -xdf && npm install"
  },
  "dependencies": {
    "node-fetch": "^2.1.2"
  }
}
